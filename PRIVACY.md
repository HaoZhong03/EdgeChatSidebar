# Edge Chat Sidebar 隐私政策

最后更新：2026 年 8 月 19 日

Edge Chat Sidebar 是一个 Microsoft Edge 侧栏扩展。它让用户在扩展设置中填写自己的模型 API 配置，并直接从侧栏发起模型对话。本政策说明扩展处理哪些数据、数据会发送到哪里，以及用户可以如何控制这些数据。

## 1. 我们不运营遥测或广告服务

扩展作者不运营收集遥测、分析、广告标识符或用户画像的服务器。扩展不会把 API Key、对话内容、系统提示词、图片或使用量发送给扩展作者。

## 2. 扩展处理的数据

扩展可能在用户设备本地处理和保存以下内容：

- 用户填写的模型 API Key、模型名称、Endpoint 和其他提供商配置；
- 系统提示词、对话消息、思考内容、消息时间、使用量信息和对话索引；
- 用户粘贴或发送的 PNG、JPEG、WebP 图片；
- 主题、时间戳、联网搜索等偏好设置。

API Key、提供商配置、系统提示词、对话、消息时间、思考内容、使用量和图片会保存在扩展自己的 IndexedDB 数据库中，并使用 AES-256-GCM 加密。扩展不会把这些敏感内容以明文写入 `chrome.storage.local`。加密只保护设备上的存储；当用户主动发送请求时，运行中的扩展必须读取相关数据才能完成请求。

## 3. 数据发送到哪些服务

只有当用户配置 API Key 或自定义提供商并发送请求时，扩展才会把完成请求所需的数据发送到用户选择的模型服务：

- DeepSeek 请求发送到 DeepSeek API；
- 小米 MiMo 请求发送到小米 MiMo API；
- 自定义提供商请求发送到用户自己填写并授权的 HTTPS Endpoint，或本机 `localhost`/`127.0.0.1` Endpoint。

发送内容可能包括对话上下文、系统提示词、模型名称、联网搜索所需的用户问题，以及在 MiMo 多模态模型中由用户主动粘贴的图片。联网搜索是否发生由用户的设置和当前模型请求决定；第三方服务如何处理这些数据受其自身隐私政策和服务条款约束。扩展作者无法控制第三方服务的保存期限或使用方式。

扩展不会把数据发送到未由用户选择或配置的模型服务，也不会把 API Key 发送给扩展作者。扩展中的第三方前端库（KaTeX、marked 和 DOMPurify）随扩展包本地提供，不从远程服务器加载代码。

## 4. 用户控制与删除

用户可以在“拓展选项”页面修改或删除 API Key、提供商、系统提示词和偏好设置；可以清理未被历史索引引用的缓存；也可以使用“清空全部本地数据”删除 API Key、提供商、系统提示词、历史对话、图片和动态访问权限。删除本地数据不会撤回已经发送给第三方模型服务的数据，相关请求应按照该服务的政策处理。

## 5. 权限用途

- `sidePanel`：在 Edge 侧栏中显示对话界面。
- `storage`：保存非敏感的界面和模型选择偏好。
- `unlimitedStorage`：为本地加密的历史对话、思考内容、使用量和用户主动发送的图片提供足够存储空间。
- 内置 API 的 Host 权限：向 DeepSeek 和小米 MiMo 的官方 API 发起用户请求。
- 可选 Host 权限：只有在用户添加自定义提供商时，才按用户填写的 HTTPS 或本机 Endpoint 请求对应的访问权限。

## 6. 政策更新与联系方式

如果扩展的数据处理方式发生变化，我们会更新本页面的“最后更新”日期和内容。有关扩展源代码或隐私政策的问题，可以通过项目主页提交 Issue：<https://github.com/HaoZhong03/EdgeChatSidebar/issues>。
